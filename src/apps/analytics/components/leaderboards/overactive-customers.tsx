import { Flex } from 'components';

import AnalyticsSystemStats from '../systemStats/analytics.system-stats';

const OveractiveCustomers = () => {
  return (
    <Flex fluid>
      <AnalyticsSystemStats />
    </Flex>
  );
};

export default OveractiveCustomers;
